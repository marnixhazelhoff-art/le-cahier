const app = document.getElementById('app');

app.innerHTML = `
  <div class="page">
    <h1>le cahier</h1>
    <p>Scaffold in place. The router and views arrive in a later build step.</p>
  </div>
`;
